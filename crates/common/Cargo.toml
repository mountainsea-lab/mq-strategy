[package]
name = "common"
version = "0.1.0"
edition = "2024"

[dependencies]
nautilus-model = { workspace = true, features = ["stubs"] }

time = {workspace = true}
log = {workspace = true}

[lints]
workspace = true
